"""Contract tests — verify that schemas and events honour their public interfaces.

These tests act as a safeguard: if a schema field name changes or an event
field is removed, the contract test breaks before any consumer is affected.
"""
