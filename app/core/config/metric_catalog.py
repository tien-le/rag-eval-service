"""Central metric catalog with definitions and tradeoffs."""

from dataclasses import dataclass


@dataclass(frozen=True)
class MetricDefinition:
    name: str
    category: str
    description: str
    when_to_use: str
    tradeoff: str
    how_to_improve: str
    common_failure_mode: str
    guardrail_or_pair_metric: str
    decision_stage: str
    target_guideline: str


METRIC_CATALOG: tuple[MetricDefinition, ...] = (
    MetricDefinition(
        name="accuracy",
        category="classification",
        description="Overall proportion of correct predictions.",
        when_to_use="Balanced classes and high-level quality checks.",
        tradeoff="Can hide minority-class failures.",
        how_to_improve="Improve label quality, rebalance data, and reduce ambiguous class definitions.",
        common_failure_mode="High score on dominant class while minority classes degrade silently.",
        guardrail_or_pair_metric="Pair with per-class recall and confusion matrix.",
        decision_stage="Early baseline and regression smoke checks.",
        target_guideline="Use as KPI only if class imbalance ratio is near 1:1.",
    ),
    MetricDefinition(
        name="precision",
        category="classification",
        description="How many predicted positives are actually positive.",
        when_to_use="False positives are costly.",
        tradeoff="May reduce recall if optimized alone.",
        how_to_improve="Tighten decision thresholds, improve negative examples, and add hard negatives.",
        common_failure_mode="Model becomes too conservative and misses true positives.",
        guardrail_or_pair_metric="Pair with recall and business false-negative cost.",
        decision_stage="Policy tuning before production cutover.",
        target_guideline="Set threshold from expected false-alert budget.",
    ),
    MetricDefinition(
        name="recall",
        category="classification",
        description="How many actual positives are correctly detected.",
        when_to_use="Missing positives is costly.",
        tradeoff="May increase false positives if optimized alone.",
        how_to_improve="Add diverse positive examples and reduce threshold conservatism.",
        common_failure_mode="Recall increases while precision collapses and operations overload.",
        guardrail_or_pair_metric="Pair with precision and reviewer throughput capacity.",
        decision_stage="Risk-sensitive production policy selection.",
        target_guideline="Tune to maximize true-positive capture within false-positive SLO.",
    ),
    MetricDefinition(
        name="f1",
        category="classification",
        description="Harmonic balance between precision and recall.",
        when_to_use="Need balance between FP and FN risk.",
        tradeoff="Less interpretable for business stakeholders.",
        how_to_improve="Jointly tune thresholds and improve both positive and negative hard samples.",
        common_failure_mode="Single blended score hides asymmetric error business impact.",
        guardrail_or_pair_metric="Pair with class-wise precision/recall and cost-weighted metrics.",
        decision_stage="Model selection in offline evaluation.",
        target_guideline="Use for ranking candidates; finalize with cost-aware thresholding.",
    ),
    MetricDefinition(
        name="context_precision",
        category="retrieval",
        description="How much retrieved context is relevant.",
        when_to_use="Evaluate retrieval ranking quality.",
        tradeoff="May not capture generation correctness.",
        how_to_improve="Improve chunking strategy, query rewriting, and reranker quality.",
        common_failure_mode="High lexical relevance but missing facts needed for answer.",
        guardrail_or_pair_metric="Pair with context_recall and faithfulness.",
        decision_stage="Retriever and reranker iteration cycle.",
        target_guideline="Increase top-k relevance without reducing answer coverage.",
    ),
    MetricDefinition(
        name="context_utilization",
        category="retrieval",
        description="How much of the retrieved context is actually used by the answer.",
        when_to_use="Detect when answers ignore retrieved evidence.",
        tradeoff="Can be high even when the used context is incomplete.",
        how_to_improve="Improve answer grounding prompts, citation instructions, and context formatting.",
        common_failure_mode="Model answers from prior knowledge and underuses retrieved passages.",
        guardrail_or_pair_metric="Pair with context_recall and faithfulness.",
        decision_stage="Prompt and grounding behavior tuning.",
        target_guideline="Increase utilization without lowering factual completeness.",
    ),
    MetricDefinition(
        name="non_llm_context_precision",
        category="retrieval",
        description="String/reference-based precision over retrieved contexts without an LLM judge.",
        when_to_use="Low-cost large-scale retrieval evaluation with reference contexts.",
        tradeoff="Insensitive to semantic similarity and paraphrases.",
        how_to_improve="Normalize text, improve chunk boundaries, and strengthen lexical matching quality.",
        common_failure_mode="Semantically relevant passages score low due to wording mismatch.",
        guardrail_or_pair_metric="Pair with context_precision and manual spot checks.",
        decision_stage="High-volume regression evaluation pipelines.",
        target_guideline="Use as fast guardrail, not as the only relevance signal.",
    ),
    MetricDefinition(
        name="id_based_context_precision",
        category="retrieval",
        description="Precision using retrieved context IDs against reference context IDs.",
        when_to_use="Datasets have stable document/chunk identifiers.",
        tradeoff="Cannot validate relevance quality if IDs are stale or mislabeled.",
        how_to_improve="Improve retrieval ranking and ID mapping consistency across indexing pipelines.",
        common_failure_mode="Index/version drift causes correct text with mismatched IDs.",
        guardrail_or_pair_metric="Pair with non_llm_context_precision and context_precision.",
        decision_stage="Retriever ranking and indexing integrity checks.",
        target_guideline="Maintain high precision while tracking index freshness.",
    ),
    MetricDefinition(
        name="context_recall",
        category="retrieval",
        description="How much required context was retrieved.",
        when_to_use="Evaluate retrieval coverage.",
        tradeoff="Can reward verbose retrieval with extra noise.",
        how_to_improve="Increase retriever recall via better embeddings, expansion, and top-k tuning.",
        common_failure_mode="Recall rises by adding noisy passages that hurt generation.",
        guardrail_or_pair_metric="Pair with context_precision and noise_sensitivity.",
        decision_stage="Retriever capacity and top-k calibration.",
        target_guideline="Improve coverage while keeping prompt token budget stable.",
    ),
    MetricDefinition(
        name="non_llm_context_recall",
        category="retrieval",
        description="Reference-based recall over required contexts without an LLM judge.",
        when_to_use="Need fast, deterministic recall checks at scale.",
        tradeoff="Misses semantically equivalent passages with different wording.",
        how_to_improve="Improve retriever coverage, query expansion, and reference set completeness.",
        common_failure_mode="True positives are undercounted when references are incomplete.",
        guardrail_or_pair_metric="Pair with context_recall and id_based_context_recall.",
        decision_stage="Retriever coverage and top-k tuning.",
        target_guideline="Track over time to detect recall regressions early.",
    ),
    MetricDefinition(
        name="id_based_context_recall",
        category="retrieval",
        description="Recall using retrieved context IDs against reference context IDs.",
        when_to_use="Ground-truth relevant context IDs are available.",
        tradeoff="Depends heavily on reference ID quality and ID stability.",
        how_to_improve="Increase retriever breadth, improve candidate generation, and maintain ID consistency.",
        common_failure_mode="Good retrieval appears poor when references or IDs are outdated.",
        guardrail_or_pair_metric="Pair with context_recall and context_precision.",
        decision_stage="Coverage validation for indexed corpora.",
        target_guideline="Use as objective recall guardrail for indexed datasets.",
    ),
    MetricDefinition(
        name="context_entity_recall",
        category="retrieval",
        description="Entity-level recall over retrieved contexts.",
        when_to_use="Entity-sensitive tasks and factual QA.",
        tradeoff="Depends on reliable entity extraction.",
        how_to_improve="Improve NER quality, entity normalization, and domain-specific synonyms.",
        common_failure_mode="Entity aliases and abbreviations undercount true retrieval quality.",
        guardrail_or_pair_metric="Pair with context_recall and domain entity dictionary audits.",
        decision_stage="Domain QA readiness for entity-heavy use cases.",
        target_guideline="Track top business entities separately from aggregate entity recall.",
    ),
    MetricDefinition(
        name="answer_relevancy",
        category="generation",
        description="How relevant the answer is to the user query.",
        when_to_use="Detect off-topic or generic answers.",
        tradeoff="Relevant answers can still be incorrect.",
        how_to_improve="Tighten system prompts, improve query understanding, and constrain answer scope.",
        common_failure_mode="Polished but shallow answers score high despite factual gaps.",
        guardrail_or_pair_metric="Pair with faithfulness and factual correctness checks.",
        decision_stage="Response quality tuning before release.",
        target_guideline="Use as minimum floor; never as sole release gate.",
    ),
    MetricDefinition(
        name="faithfulness",
        category="generation",
        description="Whether answer claims are grounded in provided context.",
        when_to_use="Hallucination risk is critical.",
        tradeoff="May penalize useful reasoning not explicit in context.",
        how_to_improve="Improve citation grounding, reduce unsupported synthesis, and refine retrieval quality.",
        common_failure_mode="Model hallucinates plausible details absent from retrieved evidence.",
        guardrail_or_pair_metric="Pair with answer_relevancy and context_recall.",
        decision_stage="Production release gate and incident prevention.",
        target_guideline="Set strict minimum threshold for high-risk domains.",
    ),
    MetricDefinition(
        name="noise_sensitivity",
        category="robustness",
        description="How fragile generation is to noisy retrieved context.",
        when_to_use="Real-world retrieval includes irrelevant documents.",
        tradeoff="Harder to optimize directly; needs targeted test sets.",
        how_to_improve="Add distractor-aware training/evals and improve context filtering/reranking.",
        common_failure_mode="Model follows noisy snippets and ignores the most relevant evidence.",
        guardrail_or_pair_metric="Pair with faithfulness and adversarial retrieval test suites.",
        decision_stage="Pre-production robustness and chaos evaluation.",
        target_guideline="Track trend over releases; block rollout on sharp regressions.",
    ),
    MetricDefinition(
        name="topic_adherence",
        category="agent",
        description="Whether the agent remains aligned with the intended topic and scope.",
        when_to_use="Agent workflows where off-topic drift is costly.",
        tradeoff="May penalize valid exploratory steps in ambiguous tasks.",
        how_to_improve="Tighten instructions, improve planning prompts, and constrain tool usage boundaries.",
        common_failure_mode="Agent follows tangential context and diverges from user intent.",
        guardrail_or_pair_metric="Pair with agent_goal_accuracy and human review traces.",
        decision_stage="Agent behavior policy tuning.",
        target_guideline="Set minimum threshold before autonomous execution rollout.",
    ),
    MetricDefinition(
        name="tool_call_accuracy",
        category="agent",
        description="How often selected tool calls are correct for the task step.",
        when_to_use="Tool-augmented agents where API/tool correctness matters.",
        tradeoff="Can overlook partial success when sequence-level behavior is flawed.",
        how_to_improve="Improve tool descriptions, function schemas, and tool-selection prompting.",
        common_failure_mode="Agent calls plausible but wrong tools or arguments.",
        guardrail_or_pair_metric="Pair with tool_call_f1 and execution success rate.",
        decision_stage="Agent-tool integration hardening.",
        target_guideline="Treat regressions as release blockers for critical automations.",
    ),
    MetricDefinition(
        name="tool_call_f1",
        category="agent",
        description="Balanced precision/recall measure for correct tool invocation decisions.",
        when_to_use="Need balanced view of missed vs incorrect tool calls.",
        tradeoff="Aggregated score can hide high-impact tool-specific failures.",
        how_to_improve="Tune tool selection prompts and add hard-negative tool examples.",
        common_failure_mode="Overall F1 looks acceptable while one critical tool fails often.",
        guardrail_or_pair_metric="Pair with per-tool confusion analysis and tool_call_accuracy.",
        decision_stage="Pre-production agent evaluation.",
        target_guideline="Use for model comparison, then verify per-tool reliability.",
    ),
    MetricDefinition(
        name="agent_goal_accuracy",
        category="agent",
        description="Whether final agent outputs satisfy the user goal.",
        when_to_use="End-to-end agent success measurement.",
        tradeoff="Can hide inefficiency or unsafe intermediate actions.",
        how_to_improve="Improve planning quality, stopping criteria, and recovery strategies.",
        common_failure_mode="Agent reaches output format but fails true task objective.",
        guardrail_or_pair_metric="Pair with topic_adherence and safety policy checks.",
        decision_stage="Final acceptance gate for agent systems.",
        target_guideline="Define goal-completion rubric before thresholding this metric.",
    ),
)

